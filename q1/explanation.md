# Question 1 Explanation

- `whoami` verified the username currently active in the shell session.
- `groups` showed the groups associated with the account and helped confirm the account's permission context.
- `echo "$SHELL"` and `pwd` confirmed the current shell and the present working directory.
- `ls -ls` listed the workspace contents so I could inspect the current directory structure.
- The network check with `curl` confirmed that outbound connectivity to YouTube was available from this environment.
